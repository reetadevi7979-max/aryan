services:
  - type: web
    name: aryan-freelancer
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:render
    startCommand: npm run start:render
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: NODE_ENV
        value: production